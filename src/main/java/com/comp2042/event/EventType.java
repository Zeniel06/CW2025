package com.comp2042.event;

public enum EventType {
    DOWN, LEFT, RIGHT, ROTATE, HARD_DROP, 
    HOLD  // Hold piece feature - stores/swaps current piece with held piece (triggered by Shift key)
}

